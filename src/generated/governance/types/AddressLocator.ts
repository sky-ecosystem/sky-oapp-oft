/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'
/**
 * This type is used to derive the {@link AddressLocator} type as well as the de/serializer.
 * However don't refer to it in your code but use the {@link AddressLocator} type instead.
 *
 * @category userTypes
 * @category enums
 * @category generated
 * @private
 */
export type AddressLocatorRecord = {
  Payer: void /* scalar variant */
  Signer: { fields: [number] }
  Address: { fields: [web3.PublicKey] }
  AltIndex: { fields: [number, number] }
}

/**
 * Union type respresenting the AddressLocator data enum defined in Rust.
 *
 * NOTE: that it includes a `__kind` property which allows to narrow types in
 * switch/if statements.
 * Additionally `isAddressLocator*` type guards are exposed below to narrow to a specific variant.
 *
 * @category userTypes
 * @category enums
 * @category generated
 */
export type AddressLocator = beet.DataEnumKeyAsKind<AddressLocatorRecord>

export const isAddressLocatorPayer = (
  x: AddressLocator
): x is AddressLocator & { __kind: 'Payer' } => x.__kind === 'Payer'
export const isAddressLocatorSigner = (
  x: AddressLocator
): x is AddressLocator & { __kind: 'Signer' } => x.__kind === 'Signer'
export const isAddressLocatorAddress = (
  x: AddressLocator
): x is AddressLocator & { __kind: 'Address' } => x.__kind === 'Address'
export const isAddressLocatorAltIndex = (
  x: AddressLocator
): x is AddressLocator & { __kind: 'AltIndex' } => x.__kind === 'AltIndex'

/**
 * @category userTypes
 * @category generated
 */
export const addressLocatorBeet = beet.dataEnum<AddressLocatorRecord>([
  ['Payer', beet.unit],
  [
    'Signer',
    new beet.BeetArgsStruct<AddressLocatorRecord['Signer']>(
      [['fields', beet.fixedSizeTuple([beet.u8])]],
      'AddressLocatorRecord["Signer"]'
    ),
  ],
  [
    'Address',
    new beet.BeetArgsStruct<AddressLocatorRecord['Address']>(
      [['fields', beet.fixedSizeTuple([beetSolana.publicKey])]],
      'AddressLocatorRecord["Address"]'
    ),
  ],
  [
    'AltIndex',
    new beet.BeetArgsStruct<AddressLocatorRecord['AltIndex']>(
      [['fields', beet.fixedSizeTuple([beet.u8, beet.u8])]],
      'AddressLocatorRecord["AltIndex"]'
    ),
  ],
]) as beet.FixableBeet<AddressLocator, AddressLocator>
